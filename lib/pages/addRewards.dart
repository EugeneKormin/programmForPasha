import 'package:flutter/material.dart';


class AddRewards extends StatelessWidget {
  const AddRewards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('Add rewards Screen')
    );
  }
}
