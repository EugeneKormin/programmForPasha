import 'package:flutter/material.dart';


class AddKm extends StatelessWidget {
  const AddKm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('Add km Screen')
    );
  }
}
